import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.mkr1',
  appName: 'mkr1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
